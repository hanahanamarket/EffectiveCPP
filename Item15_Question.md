# 設問

カウンター用の関数`hand_ticket_to_counter`は所与のものとして決まっており、伝票番号の値（`int` 型）しか受け取れない。その実装もシグネチャも変えられない。

なお、次のコードでは、`patron` の実行の際の処理の順に **ⅰ〜ⅶ** の番号を付番している。

```cpp
void hand_ticket_to_counter(int ticketNumber);                  // ⅶ

class LibraryTicket {                                           // ⅲ
    public:
        explicit LibraryTicket(int number) : number_(number) {} // ⅳ
        ~LibraryTicket() {}
        int ticketNumber() const { return number_; }
        
    private:
        int number_;                                            // ⅴ
};

void patron() {                                                 // ⅰ
    LibraryTicket mine(555);                                    // ⅱ
    hand_ticket_to_counter(mine);                               // ⅵ
}
```

**(1)** 上のコードでは、ⅵ は `hand_ticket_to_counter(mine)` がカウンター用の関数呼び出し、ⅶ は関数宣言に相当します。<br>
`LibraryTicket`のインスタンス`mine`をカウンター用の関数`hand_ticket_to_counter`に対し、**そのままでは渡せない主な理由**として、**正しいものを一つ**選べ。

- **A** `mine` は値渡しで渡されるため `LibraryTicket` オブジェクト全体がコピーされ、カウンターが要求する `int` 型の引数としては渡せないから。
- **B** `LibraryTicket` のコンストラクタに `explicit` が付いているから、関数の引数として `mine` を渡せない。
- **C** `hand_ticket_to_counter` が受け取るのは `int` であり、渡している `mine` は `LibraryTicket` という**別の型**だから、自動では `number_` に格納された `int` 値に置き換わらない。
- **D** `mine` が保持する伝票番号（`number_`）は `private` だから、コンパイラが読み取れない。

**(2)** `mine` が保持する伝票番号（`number_` の値）をカウンター関数へ渡したい。次のような二種類の対策がある。

- **対策1（明示的なリソースへのアクセス）** `mine.ticketNumber()` のように、`int` を取得する操作を**メンバ関数**で毎回書いて渡す。
- **対策2（暗黙の型変換）** `LibraryTicket` を `int` に**暗黙的に型変換できる**ようにして、`hand_ticket_to_counter(mine)` のように短く書けるようにする。

次のうち、**この二つの対策についての説明として適切なものを一つ**選べ。

- **A** 対策1 は冗長なので、可読性では常に対策2 が上である。
- **B** 対策1 と対策2 はまったく同じで、保守上の違いもない。
- **C** 対策2 を入れても `ticketNumber()` のような明示的な取り出しは引き続き書けるため、読み取りにくさの差は実質ほとんどない。
- **D** 対策2 だけを採ると、`int x = mine;` のような代入が（暗黙変換を定義すれば）許されやすく、一見は普通の代入に見えて、実は `number_` に格納された伝票番号を取り出していることが読み取りにくくなりやすい。<br>
一方、対策1 なら取り出しの意図が呼び出しの形に残りやすい。

**(3)** **この設問だけ**のコード断片について、上から実行される順に **`// ⅰ` と `// ⅱ`** を付けている（**(1) の `patron` 用の付番 ⅰ〜ⅶ とは別に数え直した番号**です）。**代入の見た目だけを比べる**と、どちらの行の方が「何をしているか」が読み取りにくいか。**正しいものを一つ**選べ。

```cpp
int copyA = mine;                   // ⅰ
int copyB = mine.ticketNumber();    // ⅱ
```

- **A** ⅱ の方が読み取りにくい。
- **B** ⅰ の方が読み取りにくい。`copyA` の右辺は代入の形が素直に見えやすく、実際には（対策2 の暗黙変換を入れた場合だけ）ラッパから伝票番号（`int`）を取り出している、と分かりにくい。
- **C** ⅰ と ⅱ はどちらも同程度に読み取りにくい。
- **D** どちらも最終的には `int` 変数に同じ伝票番号の値を入れるだけなので、「何をしているか」の読み取りのしやすさに本質的な差はない。
