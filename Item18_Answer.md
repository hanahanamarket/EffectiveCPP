# 解答

## (1) 正解：**B**
`int` だけでは `month`, `day`, `year`の区別が型システム上存在しない。
そのため、次のような順序ミスをコンパイラが検出できない。
```cpp
Date(
    30,   // クラス定義で第1引数がmonthなのに30を指定。
    3,
    2025
);
```

| 選択肢 | 評価                   |
| --- | -------------------- |
| A   | 引数名は呼び出し側の式には現れず、型チェックの根拠にならない |
| B   | 正しい                  |
| C   | class/struct は本質ではない |
| D   | 順番ではなく「意味が型にない」のが問題  |

---

## (2) 正解：**C**

`Month(13)`のような不正状態を生成できる設計が良くない。
生成可能な値そのものを限定することで不正状態、実行時チェック依存を減らせる。

| 選択肢 | 評価                         |
| --- | -------------------------- |
| A   | 実行時チェックに依存し、不正値の生成をコンパイル時に防げない |
| B   | enum は int と相互変換しやすく安全性が弱い |
| C   | 正しい                        |


---

## (3) 正解：**B**

`int` では `x + 100` も `100 + x` も書ける。
この `Money` では `price + 100` だけが通り、`100 + price` は通らない。
新しい型に演算子を与えるときは、組み込み型と同様の一貫性・対称性が必要である。

| 選択肢 | 評価 |
| --- | --- |
| A | `explicit` は暗黙変換を抑える効果があり、本問の主因ではない |
| B | 正しい |
| C | `const` は読み取り専用の意味であり、本問の主因ではない |
| D | class かどうかは本質ではない |

---

## (4) 正解：**A**

DLL をまたいで別のモジュールが `delete` すると、`new` / `delete` の実装差で問題になることがある。
`shared_ptr` に DLL A 側で定義した deleter を持たせ、DLL B では `delete` しないことで、解放は生成側の手順に任せられる。
DLL B で `shared_ptr` に包むだけでは、最後の所有者が破棄するときに DLL B の既定 deleter で `delete` されうる。

| 選択肢 | 評価 |
| --- | --- |
| A | 正しい |
| B | `shared_ptr` でも、DLL B 側で組み立てると既定の deleter により DLL B で `delete` されうる |
