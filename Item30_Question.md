# 設問

`inline` は、コンパイラに対して「関数呼び出しを、その関数の定義コードで置き換えてほしい」と**要請**する仕組みである。
要請が受理されれば関数呼び出しのオーバーヘッドを減らせるが、呼び出し箇所ごとに定義が展開されるため、コードサイズの増大やデバッグのしづらさなどのトレードオフも生じる。

各問は独立している。

---

## (1)

次の関数を考える。

```cpp
inline int sumArray(const int* data, int n)
{
    int total = 0;
    for (int i = 0; i < n; ++i)
    {
        total += data[i];
    }
    return total;
}
```

この関数は `inline` と宣言され、プログラム内の多数の箇所から呼び出される。コンパイラのふるまいに関する記述として最も適切なものはどれか。

A. `inline` と宣言されているため、すべての呼び出し箇所で必ずインライン展開される

B. `inline` は要請にすぎず、ループを含むなど複雑な関数についてはインライン化を拒否することがある

C. `inline` と宣言されているため、呼び出し箇所への展開とは別に関数本体は生成されない

D. ループを含む関数は `inline` を付けなければコンパイルできない

---

## (2)

次の関数テンプレートを考える。

```cpp
template<typename T>
T twice(T x)
{
    return x + x;
}
```

このテンプレートはヘッダファイル内に定義されている。インライン化について最も適切な説明はどれか。

A. ヘッダ内に定義されているため、具体化された各関数は自動的にインライン化される

B. テンプレートの具体化とインライン化は別の処理であり、インライン化を要請するには `inline` を付ける必要がある

C. 関数テンプレートはインライン化できない

D. クラス定義内に書かれたメンバ関数だけがインライン化の対象になる

---

## (3)

次のコードを考える。

```cpp
class Base {
public:
    virtual int compute() const;
};

void use(const Base& obj)
{
    obj.compute();
}
```

`compute` はヘッダ内で `inline` と宣言されて定義されているものと仮定する。`use` 内の `obj.compute()` がインライン展開されにくい**主な理由**として最も適切なものはどれか。

A. `inline` と宣言されているため、呼び出しは必ずインライン展開される

B. 仮想関数は実行時にどの定義が使われるか決まるが、インライン化はコンパイル時に呼び出しを定義コードで置き換えることを要請するため、両者は両立しにくい

C. `inline` と `virtual` は同時に指定できないため、コンパイルエラーになる

D. `const` 参照経由の呼び出しはインライン展開の対象外だから

---

## (4)

ライブラリがヘッダファイルで次のように `inline` 関数を提供しているとする。

```cpp
inline int libraryHelper(int x)
{
    return x * 2;
}
```

クライアントプログラムはこのヘッダをインクルードして `libraryHelper` を利用している。ライブラリ作者が `libraryHelper` の**関数本体だけ**を変更し、公開インタフェース（引数・戻り値の型など）は変えなかった場合、クライアント側で必要な対応として最も適切なものはどれか。

A. クライアントの再コンパイルは不要で、ライブラリの再リンクだけでよい

B. インライン関数は呼び出し側でコンパイルされるため、クライアント側を再コンパイルする必要がある

C. 動的リンクを使っていれば、ライブラリのバイナリを差し替えるだけでよい

D. ヘッダをインクルードしている以上、変更は自動的に反映される
