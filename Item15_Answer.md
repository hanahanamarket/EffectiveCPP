# 模範解答

## (1) 正解：**C**

### 答えの要点

関数 `hand_ticket_to_counter` の第 1 引数は **ⅶ** の宣言どおり `int` のみを受け取る。一方 **`mine` の式の型は `LibraryTicket`** であり、言語上は `int` とは別の型名として扱われる。コンパイラは、特別な規則がない限り「`LibraryTicket` を渡したら、メンバ `number_` に格納された `int` をそっと差し替える」などはしてくれない。したがって **ⅵ** の呼び出しは、**パラメータ型と実引数の型が一致しない**ことがそのままコンパイルエラー（など）の原因になる。

### 各肢の解説

| 選択肢 | 内容 |
|--------|------|
| **A** | **誤り**。値渡しでオブジェクトがコピーされること自体は C++ では普通に起こりうる。問題は**`LibraryTicket` を `int` 引数へ渡す段階で型が合わずコンパイルできない**ことにある。主因は「`LibraryTicket` オブジェクト全体か `int` 値か」ではなく、**宣言された引数の型と実引数の型の不一致**。 |
| **B** | **誤り**。`explicit` は「渡した `int` から `LibraryTicket` を暗黙につくる」側の抑制に効くことが多い。今回の問題は「`LibraryTicket` を `int` が欲しいところへ渡した」ことによる**型の不一致**であり、主因の説明にはならない。 |
| **C** | **正しい**。下位 API が `int` のみを受ける限り、ラッパを抱えたままでは型が噛み合わない。あとから **対策1**（名前付き取得）や **対策2**（暗黙変換など）で、「いつ・どう `int` に橋渡しするか」を設計する話になる。 |
| **D** | **誤り**。`private` は**他の関数がメンバへ直接アクセスできない**という意味であり、コンパイラが型検査をする能力とは別事。型さえ合っていれば、アクセサ経由で中身を読む設計にできる。 |

---

## (2) 正解：**D**

### 答えの要点

**対策1** は `ticketNumber()` のように、**生の `int` に近い値を取り出していることが呼び出しの形に残る**。**対策2** は便利だが、変換演算子などで `LibraryTicket` が「`int` として求められる場所」に広く入り込めるようになる。すると `int x = mine;` のように、**代入の形は淡々としているのに、実は `number_` の `int` 値だけを抜き出している**読み方になり、意図や所有のイメージがコードから追いにくくなりやすい。設問 **D** は、そのトレードオフを言い当てている。

### 各肢の解説

| 選択肢 | 内容 |
|--------|------|
| **A** | **誤り**。「短ければ常によい」ではない。読み手が何をしているか追いやすさ・誤用のしにくさとの兼ね合いで、対策1 が好まれる場面もある。 |
| **B** | **誤り**。取り出し方が名前付きか暗黙かで、読みやすさやバグりやすさの傾向は違う。 |
| **C** | **誤り**。対策2 を入れても対策1 の書き方を併用できるのはその通りだが、暗黙変換が存在すると `int x = mine` のような文脈にも変換が効き、**取り出しの意図が字面に残りにくい**という差は残る。 |
| **D** | **正しい**。暗黙変換を多用できる設計は書き味は短くなる一方、想定外の文脈にも型が紛れ込みやすく、**「生のリソース（ここでは伝票番号の `int` 値）を取り出した」ことの目印**が弱くなりがち、という指摘として妥当。 |

---

## (3) 正解：**B**

### 答えの要点

比較の前提は問題文どおり。**ⅰ** は（対策2 のように **`LibraryTicket` から `int` への暗黙変換が定義されている想定でのみ**）代入が成立する行である。**ⅱ** は `mine.ticketNumber()` と書かれており、**名前付きのメンバ呼び出し**がそのまま残る。代入の見た目だけ見るなら **ⅰ** の方が「ただ値を代入している」ように見えやすく、**ⅱ** の方が「何らかの操作で値を取得している」と読みやすい。したがって「何をしているか」の読み取りのむずかしさについて **B** が妥当。**ⅱ** が読みにくい、とする **A・C・D** は問題のねらいと合わない。

### 各肢の解説

| 選択肢 | 内容 |
|--------|------|
| **A** | **誤り**。**ⅱ** はメソッド名が見えるので、一般に追いやすい。 |
| **B** | **正しい**。暗黙変換に頼ると、右辺がラッパ型のまま `int` 文脈に置かれ、**代入の字面だけでは気づきにくい**ことがある。 |
| **C** | **誤り**。上記と同様、字面の情報量が違う。 |
| **D** | **誤り**。結果として同じ `int` 値が入ることと、**代入の字面から操作の意図が読み取れるか**は別問題。**ⅱ** はメソッド名が残るが、**ⅰ** は暗黙変換があると単なる代入に見えやすい。 |

---

## 全体のつながり（まとめ）

- **(1)** … 変更できない API が **裸のハンドル／プリミティブ（ここでは `int`）** しか受け取らないとき、**所有権付きラッパ（`LibraryTicket`）とは型が異なる**。そのギャップをどう埋めるかが設計の論点になる。  
- **(2)(3)** … 埋める手段として **明示的な `get()` クラスのアクセサ**と、**暗黙の型変換**があり、後者は短く書ける反面、代入や関数呼び出しに**気づかれにくく紛れ込む**というコストがある、という話を短い問題で確認している。
