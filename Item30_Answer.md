# 模範解答

## (1) 正解：B

### この問題で確認したいこと

`inline` はコンパイラへの要請であり、必ずインライン化されるわけではない。

### 解説

`inline` は「この関数を呼び出し箇所に展開してほしい」という**要請**にすぎない。コンパイラは、ループや再帰など**複雑な関数**については、コードサイズの増大を避けるためインライン化を拒否することがある。

`sumArray` はループを含むため、多数の呼び出し箇所があっても、すべてがインライン展開される保証はない。

|選択肢|判定|理由|
|-------|-------|-------|
|A|×|`inline` は命令ではなく、必ず展開されるわけではない|
|B|○|複雑な関数はインライン化を拒否されうる|
|C|×|関数のアドレスが取られる場合など、呼び出し箇所への展開とは別に関数本体が生成されることがある|
|D|×|ループの有無と `inline` の可否は無関係|

---

## (2) 正解：B

### この問題で確認したいこと

テンプレートの具体化とインライン化は別の処理である。

### 解説

関数テンプレートをヘッダに定義するのは、具体化時にコンパイラが定義を見られるようにするためである。これはインライン化の要請とは無関係である。

テンプレートから生成された関数をインライン化したい場合は、テンプレート定義に `inline` を明示する必要がある。逆に、具体化時に特にインライン化を望まないなら、`inline` を付けない。

|選択肢|判定|理由|
|-------|-------|-------|
|A|×|ヘッダ内定義は具体化のためであり、自動インライン化を意味しない|
|B|○|具体化とインライン化は別処理で、`inline` が要請になる|
|C|×|テンプレート関数もインライン化の対象になりうる|
|D|×|クラス外の関数テンプレートも `inline` 指定で要請できる|

---

## (3) 正解：B

### この問題で確認したいこと

仮想関数の動的束縛とインライン化の衝突を理解する。

### 解説

`inline` はコンパイル時に呼び出しを定義コードで置き換えることを要請する。一方、仮想関数は**実行時**にオブジェクトの実際の型に応じて呼び出す定義が決まる。

`const Base&` 経由で `compute()` を呼ぶ場合、コンパイル時点では最終的にどの `compute` が実行されるか確定しない。インライン化は「コンパイル時に置き換える」処理、仮想呼び出しは「実行時に実装を決める」処理であり、本質的に衝突する。

`inline` はコンパイル時に呼び出しを関数本体へ置き換えることを要請する。一方、仮想関数は実行時に呼び出す関数が決まるため、両者は両立しにくい。

|選択肢|判定|理由|
|-------|-------|-------|
|A|×|仮想呼び出しは実行時に実体が決まるため、必ず展開されるわけではない|
|B|○|コンパイル時置換と実行時決定が衝突する|
|C|×|`inline virtual` は文法上問題ない|
|D|×|`const` 参照であること自体がインライン化を禁止するわけではない|

---

## (4) 正解：B

### この問題で確認したいこと

インライン関数がライブラリ更新時にクライアントへ与える影響を理解する。

### 解説

インライン関数はヘッダに定義を書いて**各翻訳単位でコンパイルされる**。そのため、ライブラリ作者が関数本体を変更すると、クライアントは**古い定義を前提として生成されたオブジェクトコード**を持ち続ける。変更を反映するには、クライアント側の**再コンパイル**が必要である。

非インライン関数であれば、リンクのし直しや動的リンクによるライブラリ差し替えで対応できる場合があるが、インライン関数にはその利点がない。

|選択肢|判定|理由|
|-------|-------|-------|
|A|×|定義は呼び出し側でコンパイルされるため、再リンクだけでは不十分|
|B|○|呼び出し側でコンパイルされるため、クライアントの再コンパイルが必要|
|C|×|クライアント側のオブジェクトコードは古い定義のまま残るため、ライブラリ差し替えだけでは更新されない|
|D|×|再コンパイルなしでは新しい定義は反映されない|
