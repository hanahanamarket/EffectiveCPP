# 解答

## 出題意図
この問題では、25項の論理展開とそこから導き出される結論の理解を前提とした問題である。

### 論理展開
1. 標準の `std::swap` はコピー3回で交換する。
2. コピーが高価な型では非効率になる。
3. 交換したいのはオブジェクト全体ではなく内部リソースである。
4. そのため効率的なメンバ `swap` を実装する。
5. 非メンバ `swap` からメンバ `swap` を呼び出す。
6. クラスなら `std::swap` を特殊化する。
7. クラステンプレートでは特殊化できないため、同じ名前空間に非メンバ `swap` を置く。
8. ADLにより最適な `swap` を選択させる。
9. テンプレート内では `using std::swap; swap(a, b);` を使う。
10. メンバ `swap` は例外を投げないようにする。

### 結論
コピーコストの高い型では、例外を投げない効率的な `swap` を提供し、ADL(Argument Dependent Lookup) によってその `swap` が選択されるように実装・利用する。

## (1) 正解：**B**

### 答えの要点

pimplイディオムの `Widget` は、値の実体を `pImpl`（ポインタ1本）で持つ。値の入れ替えは**この `pImpl` を交換するだけ**で済むのに、既定の `std::swap` は「`temp` へコピー → 代入 → 代入」と、**`Widget` とその指す `WidgetImpl` をまるごと3回コピー**してしまう。`WidgetImpl` のコピーが高コストな分だけ、これは無駄が大きい。だから「自分の型用に、ポインタだけを入れ替える効率的な `swap`」を提供する動機が生まれる。

### 各肢の解説

| 選択肢 | 内容 |
|--------|------|
| **A** | **誤り**。既定実装はコピーと代入だけを使い、`pImpl` を直接触らないのでコンパイルは通る。`private` は問題の主因ではない。 |
| **B** | **正しい**。ポインタ交換で済むのに丸ごと3回コピーする**非効率**が主な問題。 |
| **C** | **誤り**。既定 `swap` が必ず例外を投げるわけではない。問題は安全性ではなく効率である。 |
| **D** | **誤り**。既定実装は**ポインタ交換をしてくれない**（丸ごとコピーする）ので「常に最速」ではない。 |

---

## (2) 正解：**B**

### 答えの要点

利用側が `swap(a, b)` を**修飾なし**で呼び、手前で `using std::swap;` をしておくと、コンパイラは**実引数依存探索（ADL／Koenig探索）**により、まず `a`・`b` の型と同じ名前空間にある専用 `swap` を探し、見つからなければ `std::swap` を使う。したがって「型専用があればそれ、なければ標準」を自動で選べる。これを成立させるには、専用 `swap` を**クラスと同じ名前空間の非メンバ関数**として置けばよい（その中で public メンバ `swap` を呼ぶ）。

クラス**テンプレート**には別の制約がある。関数テンプレートの**部分特化はできず**、また `std` 名前空間に**新しいテンプレートを追加することは許されない**（未定義動作）。そのため `std::swap` の特化や追加では解決できない。

### 各肢の解説

| 選択肢 | 内容 |
|--------|------|
| **A** | **誤り**。関数テンプレートの**部分特化はできない**。さらに `std::swap(a,b)` と修飾すると、専用 `swap` が無視され `std` のものに限定されてしまう。 |
| **B** | **正しい**。同じ名前空間の非メンバ `swap` ＋ `using std::swap;` ＋ 修飾なし呼び出しで、ADL により最適な `swap` が選ばれる。 |
| **C** | **誤り**。`std::` を付けて修飾すると、`std` の `swap`（とその特化）だけに限定され、型専用の効率的な `swap` が使われなくなる。 |
| **D** | **誤り**。`std` 名前空間に新しいテンプレートを**追加するのは許されず**、未定義動作になる。 |
